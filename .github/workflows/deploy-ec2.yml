name: Deploy to AWS EC2

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  IMAGE_NAME: memoryonline-signalr
  CONTAINER_NAME: memoryonline-signalr
  EC2_PUBLIC_IP: "${{ secrets.DUCKDNS_DOMAIN }}.duckdns.org"

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Set up QEMU for ARM emulation
        uses: docker/setup-qemu-action@v3

      - name: Build Docker image for ARM64
        run: |
          docker buildx build --platform linux/arm64 -t ${{ env.IMAGE_NAME }}:latest -f src/01.Apis/MemoryOnline.Apis.Signalr/Dockerfile --load .

      - name: Save Docker image as tar
        run: docker save ${{ env.IMAGE_NAME }}:latest | gzip > image.tar.gz

      - name: Copy files to EC2
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ env.EC2_PUBLIC_IP }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          source: "image.tar.gz,.github/scripts/*"
          target: "/home/${{ secrets.EC2_USER }}"

      - name: Deploy on EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ env.EC2_PUBLIC_IP }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          command_timeout: 10m
          script: |
            echo "📂 Preparando archivos..."

            # Copiar templates a /tmp
            cp /home/${{ secrets.EC2_USER }}/.github/scripts/nginx-http.conf.template /tmp/
            cp /home/${{ secrets.EC2_USER }}/.github/scripts/nginx-https.conf.template /tmp/

            echo "✅ Templates copiados a /tmp"

            # Dar permisos y ejecutar script
            chmod +x /home/${{ secrets.EC2_USER }}/.github/scripts/deploy.sh

            echo "🚀 Ejecutando deploy.sh..."

            # Ejecutar con variables de entorno
            export DUCKDNS_DOMAIN="${{ secrets.DUCKDNS_DOMAIN }}"
            export DUCKDNS_TOKEN="${{ secrets.DUCKDNS_TOKEN }}"
            export EC2_USER="${{ secrets.EC2_USER }}"
            export CERTBOT_EMAIL="${{ secrets.CERTBOT_EMAIL }}"
            export CONTAINER_NAME="${{ env.CONTAINER_NAME }}"
            export IMAGE_NAME="${{ env.IMAGE_NAME }}"

            /home/${{ secrets.EC2_USER }}/.github/scripts/deploy.sh

            echo "🧹 Limpiando..."
            rm -rf /home/${{ secrets.EC2_USER }}/.github

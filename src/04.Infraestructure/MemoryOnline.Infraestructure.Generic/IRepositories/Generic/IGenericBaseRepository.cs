namespace MemoryOnline.Infraestructure.Generic.IRepositories.Generic
{
    /*
     * Base interface para que tanto el read como el write contengan estas funciones. 
     * Asi como el generic tb contendrá las funciones por ser write y read.
     * 
     */
    public interface IGenericBaseRepository<TEntity>  
    {
        //void Dettach(TEntity entityToAttach);
        //void DetachAllEntries();
        //void SetContext(object context);
    }
}
